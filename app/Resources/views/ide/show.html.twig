{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ide</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ide.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ide.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ide_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ide_edit', { 'id': ide.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
